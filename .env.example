# Robinhood MCP server credentials — copy to .env and fill in real values.
# NEVER commit .env to version control.

ROBINHOOD_MCP_API_KEY=your_key_here
ROBINHOOD_MCP_HOST=localhost
ROBINHOOD_MCP_PORT=8000

# Optional: override log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
